export interface Vehicle {
  id?: number; // ID del vehículo (opcional)
  brandId: number; // ID de la marca
  model: string; // Modelo del vehículo
  imageUrl: string; // Enlace de la imagen (string)
}
